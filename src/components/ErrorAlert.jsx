const ErrorAlert = () => {
  return (
    <div id="error-alert">
      There seems to be a problem getting your location.. Please check if it's
      typed in correctly.
    </div>
  );
};

export default ErrorAlert;
